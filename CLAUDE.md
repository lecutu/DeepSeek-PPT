# PPT Reflex — PPT 生成

**生成 PPT 一律调用 `/ppt` skill（ppt-maker）。** 完整 API、美学宪法、反 AI 规则、零错误配方全在 `.claude/skills/ppt-maker/SKILL.md`，用之前先读它。

```python
from ppt_reflex.builder import PPTBuilder  # 唯一合法入口
```
